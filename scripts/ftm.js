export default {
    host : 'ftm.nl',

    css : `
        .blockingOverlay {
            display: none !important;
        }

        body.hasBlockingOverlay {
            overflow: auto !important;
        }
    `
}